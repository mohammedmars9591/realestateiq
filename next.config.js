/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  /*
  // Optional: If you want to use the /src directory structure
  experimental: {
    appDir: true,
  },
  */
};

export default nextConfig;
